# Application
BASE_URL=https://www.saucedemo.com

# Test Users
STANDARD_USER=standard_user
LOCKED_OUT_USER=locked_out_user

# Password stored as Base64 (encode with: Buffer.from('your_password').toString('base64'))
PASSWORD=

# Browser config: chromium | firefox | webkit
BROWSER=chromium
HEADLESS=false
